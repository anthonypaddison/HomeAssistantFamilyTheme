type: calendar
entities:
  - calendar.family
  - calendar.anthony
  - calendar.joy
  - calendar.lizzie
  - calendar.toby
card_mod:
  style: |
    ha-card {
      height: calc(100vh - var(--header-height));
      display: flex; flex-direction: column;
    }
    ha-card > *:last-child { flex: 1 1 auto; }
