FamilyBoard:
  # … all your existing variables …

  card-mod-theme: FamilyBoard

  # --- Global card-mod hooks ---
  card-mod-view: |
    /* Keep this if needed, but keep selectors light */
    :host {
      --grid-card-gap: 0px !important;
      margin: 0 !important;
      --mdc-theme-primary: var(--primary-color);
      layout-margin: 0px !important;
      --layout-margin: 0px !important;
    }

  # Temporarily disable this block if editor crashes persist:
  # card-mod-root-yaml: |
  #   hui-root$ hui-view$ :host { padding: 0 !important; }
  #   hui-root$ hui-view$ #view { padding: 0 !important; margin: 0 !important; }

  card-mod-root: |
    :host {
      --mdc-theme-primary: var(--primary-color);
      layout-margin: 0px !important;
      --layout-margin: 0px !important;
    }
    app-header, .toolbar {
      background: var(--app-header-background-color);
      color: var(--app-header-text-color);
      box-shadow: 0 1px 0 0 var(--divider-color);
    }
    ::-webkit-scrollbar { width: 8px; height: 8px; }
    ::-webkit-scrollbar-thumb {
      background: var(--scrollbar-thumb-color);
      border: 2px solid transparent;
      background-clip: padding-box;
    }
