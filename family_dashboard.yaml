# title: Family
# theme: FamilyBoard-Base
# button_card_templates: !include /config/button_card_templates.yaml
# views:
#   - title: Family
#     path: family
#     # panel: true        # ← Optional: I'd remove this when using custom:grid-layout
#     type: custom:grid-layout
#     card_mod:
#       style: |
#         :host {
#           height: calc(100vh - var(--header-height));
#           block-size: calc(100vh - var(--header-height));
#           width: 100vw;
#           margin: 0 !important;
#           padding: 0 !important;
#           background: green !important;
#         }
#         hui-view {
#           height: 100% !important;
#           padding: 0 !important;
#           margin: 0 !important;
#         }
#         #root, .layout, .grid, .horizontal, .vertical {
#           height: 100% !important;
#           margin: 0 !important;
#           padding: 0 !important;
#         }
#     layout:
#       margin: 0px
#       padding: 0px
#       grid-template-columns: 6% 1fr
#       grid-template-rows: auto auto 1fr
#       grid-template-areas: |
#         "sidebar heading"
#         "sidebar chips"
#         "sidebar main"
#       gap: 0px
#       column-gap: 0px
#       row-gap: 0px

#     cards:
#       # - !include /config/sidebar_menu.yaml
#       - !include /config/sidebar_menu_test.yaml


title: Family
theme: FamilyBoard-Base

# Existing external include
# button_card_templates: !include button_card_templates.yaml

# TEMP: Add one inline template to prove visibility from this dashboard
button_card_templates:
  debug_inline_icon:
    show_name: false
    show_icon: true
    styles:
      card:
        - height: 64px
        - background: "#B9FBC0"   # mint
        - color: "#0F172A"
        - place-items: center
      icon:
        - width: 36px
        - height: 36px

views:
  - title: Family
    path: family
    type: custom:grid-layout
    layout:
      grid-template-columns: 120px 1fr
      grid-template-areas: |
        "sidebar main"
    cards:
      - !include sidebar_menu_test.yaml
      - type: markdown
        view_layout: { grid-area: main }
        content: "Main area"

# # 2) HEADING ROW
# - !include /config/heading_row.yaml

# # 3) CHIPS ROW
# - !include /config/chips_row.yaml

# # 4) MAIN AREA (conditional sections)
# - type: vertical-stack
#   view_layout: { grid-area: main }
#   cards:
#     # Calendar Section
#     - type: conditional
#       conditions:
#         - entity: input_select.family_section
#           state: Calendar
#       card: !include /config/calendar_section.yaml

#     # Chores Section
#     - type: conditional
#       conditions:
#         - entity: input_select.family_section
#           state: Chores
#       card: !include /config/chores_section.yaml

# # 5) FAB BUTTON (always present)
# - !include /config/fab_button.yaml
